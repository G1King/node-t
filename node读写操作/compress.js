// var fs = require('fs');
// var lib = require('zlib');
// fs.createReadStream('./input.txt').pipe(lib.createGzip()).pipe(fs.createWriteStream('input.txt.gz'));
exports.world = function () {
    console.log ('hello world');
}